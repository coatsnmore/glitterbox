---
import glitterboxLogo from '../assets/glitterbox_combined_transparent.png';
import spacecatLogo from '../assets/spacecat_logo_glitterbox_style_transparent.png';
---

<div id="container">
	<div id="background"></div>
	<main>
		<section id="hero">
			<div class="brand-header">
				<img src={spacecatLogo.src} alt="SpaceCat" class="brand-logo" />
			</div>
			<div class="logo">
				<img src={glitterboxLogo.src} alt="Glitterbox" class="logo-image" />
				<p class="tagline">Games & Tech Demos</p>
			</div>
			<div class="description">
				<p>Welcome to my digital playground where I showcase interactive games, tech experiments, and creative coding projects.</p>
			</div>
			<section id="links">
				<a class="button primary" href="/forest">🌲 Forest Game</a>
				<a class="button primary" href="/space">🚀 Space Game</a>
				<a class="button primary" href="/vampire">🧛 Vampire Survivors</a>
				<a class="button primary" href="/wellness">💚 Wellness Tracker</a>
				<a class="button secondary" href="https://spacecat.org">🌐 Spacecat.org</a>
				<a class="button secondary" href="https://github.com" target="_blank">📦 GitHub</a>
			</section>
		</section>
	</main>

	<div id="news" class="box">
		<div class="news-content">
			<h2>🎮 Latest Projects</h2>
			<p>
				Explore interactive demos and games built with modern web technologies. 
				More projects coming soon!
			</p>
		</div>
	</div>
</div>

<style>
	#background {
		position: fixed;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		z-index: -1;
		background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
		opacity: 0.1;
	}

	#container {
		font-family: 'Inter', 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
		height: 100vh;
		background: linear-gradient(135deg, #1a1a2e 0%, #16213e 50%, #0f3460 100%);
		color: white;
		position: relative;
		overflow: hidden;
	}

	#container::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		background: 
			radial-gradient(circle at 20% 80%, rgba(120, 119, 198, 0.3) 0%, transparent 50%),
			radial-gradient(circle at 80% 20%, rgba(255, 119, 198, 0.3) 0%, transparent 50%),
			radial-gradient(circle at 40% 40%, rgba(120, 219, 255, 0.2) 0%, transparent 50%);
		z-index: 0;
	}

	main {
		height: 100%;
		display: flex;
		justify-content: center;
		align-items: center;
		position: relative;
		z-index: 1;
		flex-direction: column;
	}

	.brand-header {
		position: absolute;
		top: 2rem;
		left: 2rem;
		z-index: 2;
	}

	.brand-logo {
		max-width: 120px;
		height: auto;
		filter: drop-shadow(0 0 8px rgba(255, 255, 255, 0.2));
		transition: all 0.3s ease;
		opacity: 0.9;
		mix-blend-mode: screen;
	}

	.brand-logo:hover {
		opacity: 1;
		filter: drop-shadow(0 0 12px rgba(255, 255, 255, 0.4));
		transform: scale(1.05);
	}

	#hero {
		display: flex;
		align-items: center;
		flex-direction: column;
		justify-content: center;
		padding: 16px;
		text-align: center;
		max-width: 600px;
	}

	.logo {
		margin-bottom: 2rem;
		display: flex;
		flex-direction: column;
		align-items: center;
	}

	.logo-image {
		max-width: 300px;
		height: auto;
		margin-bottom: 1rem;
		filter: drop-shadow(0 0 20px rgba(255, 255, 255, 0.2));
		transition: all 0.3s ease;
	}

	.logo-image:hover {
		filter: drop-shadow(0 0 30px rgba(255, 255, 255, 0.4));
		transform: scale(1.05);
	}

	.tagline {
		font-size: 1.5rem;
		margin: 0.5rem 0 0 0;
		color: #a8b2d1;
		font-weight: 300;
		letter-spacing: 0.1em;
	}

	.description {
		margin-bottom: 3rem;
	}

	.description p {
		font-size: 1.2rem;
		line-height: 1.6;
		color: #cbd5e1;
		margin: 0;
		max-width: 500px;
	}

	#links {
		display: flex;
		gap: 1rem;
		flex-wrap: wrap;
		justify-content: center;
	}

	#links a {
		display: flex;
		align-items: center;
		padding: 1rem 1.5rem;
		text-decoration: none;
		border-radius: 12px;
		font-weight: 600;
		font-size: 1rem;
		transition: all 0.3s ease;
		backdrop-filter: blur(10px);
		border: 1px solid rgba(255, 255, 255, 0.1);
	}

	#links a.primary {
		background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
		color: white;
		box-shadow: 0 8px 32px rgba(102, 126, 234, 0.3);
	}

	#links a.primary:hover {
		transform: translateY(-2px);
		box-shadow: 0 12px 40px rgba(102, 126, 234, 0.4);
	}

	#links a.secondary {
		background: rgba(255, 255, 255, 0.1);
		color: #e2e8f0;
		border: 1px solid rgba(255, 255, 255, 0.2);
	}

	#links a.secondary:hover {
		background: rgba(255, 255, 255, 0.15);
		transform: translateY(-1px);
	}

	.box {
		position: absolute;
		bottom: 2rem;
		right: 2rem;
		max-width: 300px;
		backdrop-filter: blur(20px);
		border: 1px solid rgba(255, 255, 255, 0.1);
		border-radius: 16px;
		background: rgba(255, 255, 255, 0.05);
		transition: all 0.3s ease;
	}

	.box:hover {
		background: rgba(255, 255, 255, 0.1);
		transform: translateY(-2px);
	}

	.news-content {
		padding: 1.5rem;
	}

	.news-content h2 {
		margin: 0 0 0.5rem 0;
		font-size: 1.2rem;
		font-weight: 600;
		color: #e2e8f0;
	}

	.news-content p {
		margin: 0;
		color: #a8b2d1;
		font-size: 0.9rem;
		line-height: 1.5;
	}

	@media screen and (max-width: 768px) {
		.brand-header {
			position: relative;
			top: auto;
			left: auto;
			margin-bottom: 1rem;
		}

		.brand-logo {
			max-width: 80px;
		}

		.logo-image {
			max-width: 250px;
		}

		.tagline {
			font-size: 1.2rem;
		}

		.description p {
			font-size: 1rem;
		}

		#links {
			flex-direction: column;
			align-items: center;
		}

		#links a {
			width: 200px;
			justify-content: center;
		}

		.box {
			position: relative;
			bottom: auto;
			right: auto;
			margin: 2rem auto 0;
			max-width: 90%;
		}
	}

	@media screen and (max-height: 600px) {
		.box {
			display: none;
		}
	}
</style>
